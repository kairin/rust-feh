# B-2 — Branch hygiene

Satisfies: .agents/SPEC.md §B-2. Status: todo. Depends: B-1 merged.

## Acceptance criteria
- Exactly these branches deleted, locally and on origin where a remote copy exists:
  `014-multi-feh-clipboard`, `20260628-070300-chore-env-dotfiles-example`,
  `feat/window-viewer-stability-validation`, `fix/local-dev-env-and-jpeg-fallback`, `pr143`.
- `git branch -a` afterwards shows only `main` + any live task-set branches.
- BOARD.md evidence log gains the deletion record (who/when/SHAs at deletion).

## Breakdown (single Sonnet orchestrator, no Haiku needed — mechanical + destructive)
1. Re-run the audit commands (git branch --merged, diff main..branch --stat) to confirm
   nothing changed since 2026-07-05.
2. Record tip SHAs of all 5 branches in BOARD.md (recovery pointer; reflog/objects persist).
3. Delete local (`git branch -D`) + remote (`git push origin --delete`) copies.

## Scope
In: the 5 listed branches only. Out: any other branch; any tag; any history rewrite.
Destructive-action rule: if any re-audit result differs from the recorded evidence, STOP
and surface instead of deleting.
