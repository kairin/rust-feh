# B-4 — Fresh verification baseline

Satisfies: .agents/SPEC.md §B-4. Status: todo. Depends: B-1 merged (run on updated main).

## Acceptance criteria
- On main after PR #145: `cargo test` (record pass/fail/ignored counts), `cargo clippy --
  -D warnings` clean, `cargo fmt --check` clean, `cargo build --release` succeeds.
- Results appended to .agents/ENV.md §"B-4 fresh verification run" with date + toolchain.
- One-sentence explanation of the "2 ignored (docs) vs 0 #[ignore] markers (static count)"
  discrepancy (inspect how the venice-scan/HEIC tests skip — env-var gate? cfg? removed?).

## Breakdown
Single Sonnet orchestrator, no fan-out: run commands, read the two skip-suspect tests,
write ENV.md addendum. No code changes. If anything is red: STOP, report, do not fix
unprompted (fix is a new task with its own approval).

## Scope
In: read-only build/test + ENV.md append. Out: dependency bumps, test edits, code changes.
