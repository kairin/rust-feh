# Tasks: Inspector UX Rework

**Input**: Design documents from `/specs/018-inspector-ux-rework/`
**Prerequisites**: spec.md, plan.md

**Board rules**: single writer = owning orchestrator. Batch 0 is prerequisite-only (bug fixes + SpecKit scaffolding); Batches 1–6 are incremental rework, each checkpointing `cargo test` green before advancing.

---

## Batch 0 — Pre-work fixes + SpecKit scaffolding (MUST land before rework)

- [x] 0.1 [S] Create specs/018-inspector-ux-rework/ + branch 018-inspector-ux-rework — folder+branch exist; acceptance: `ls -d specs/018-inspector-ux-rework/` returns the directory, `git branch | grep 018-inspector-ux-rework` shows the branch checked out — 2026-07-12: branch was already checked out by the coordinator; this spec/plan/tasks scaffolding landed in `70d0da0`.
- [x] 0.2 [S] Back-fill retro specs/017-lazy-folder-scanning/ (spec+tasks+handback: deferred per-entry count cache, F1-F6 follow-ups) — folder exists, handback lists F1-F6; acceptance: 017's spec.md/tasks.md/handback section complete, F1-F6 referenced in Batch 0 tasks below — 2026-07-12: done via haiku-implementer; `specs/017-lazy-folder-scanning/spec.md` + `tasks.md` (Handback section lists deferred count cache + F1-F6), landed in `70d0da0`.
- [x] 0.3 [S] Refresh stale 016 BOARD row; resolve BACKLOG C-2 "likely 017" numbering collision — no dangling 017 ref for image-tools; 016 row current; acceptance: `.agents/BOARD.md` shows 016 merged or in-flight, 017 reference removes the image-tools ambiguity — 2026-07-12: done via haiku-implementer; 016 row corrected to **done** (PR #151 merged, `2da6097`), 017/018 rows added, BACKLOG.md + ROUTING.md C-2 renumbered to likely feature 019 (both files, for consistency), AGENTS.md SpecKit stub repointed to 018. Landed in `70d0da0`.
- [x] 0.4 [O-review][H] F1: merge Converted snapshot by path instead of wholesale self.images replace (merge_converted_statuses in ui_logic.rs) — ScanMsg::Converted arm in main.rs; acceptance: user-added/renamed entries survive a background Converted refresh, only matching-path status flips — 2026-07-12: **escalated to opus-specialist per dispatch instruction** (trigger: concurrency/data-integrity, GUARDRAILS.md (b)), designed and implemented directly (not handed to Haiku). `merge_converted_statuses(images: &mut [ImageEntry], converted_snapshot: &[ImageEntry])` in `src/ui_logic.rs` — deliberate signature deviation from the `&mut Vec<ImageEntry>` originally specified: a `&mut [ImageEntry]` slice is clippy-`ptr_arg`-clean AND makes the "entry count never changes" invariant hold by construction (a slice cannot push/remove). Call site swapped in `ScanMsg::Converted` (main.rs); `Partial`/`Complete` arms untouched. Landed in `b2aef05`.
- [x] 0.5 [H] F1 tests: unit test merge_converted_statuses (extra entry preserved, path match flips status, no add/remove) — tests/unit or in-crate ui_logic.rs tests; green — 2026-07-12: 5 tests (unmatched-entry preserved, matched-status flips, count-preserving/no-leak, no resurrection of snapshot-only path, asset_status/size_bytes preserved on match) landed with 0.4 in `b2aef05`.
- [x] 0.6 [H] F3: inspector_width cache stores only measured max_text keyed on pixels_per_point, re-clamps to live inspector_max_width every call — main.rs inspector_width; acceptance: width tracks window resize, no per-frame font relayout — 2026-07-12: done via haiku-implementer; cache now stores only the measured text width, clamp recomputed every call from the live viewport. Landed in `b2aef05`.
- [x] 0.7 [H] F4: replace meaningless "Launched feh on {status}" success message with a count/folder-based message — main.rs launch_entry_feh; acceptance: status no longer prints "Launched feh on Ready" — 2026-07-12: done via haiku-implementer; `format!("Launched feh ({count} images)")`. Landed in `b2aef05`.
- [x] 0.8 [H] F5: pending_flat_scroll_offset peek-then-take so a not-yet-rendered target row doesn't consume the pending scroll and lose it — main.rs; acceptance: cross-folder round-trip scrolls to the landed image on the frame it appears — 2026-07-12: done via haiku-implementer; `.take()` → `.clone()` + explicit clear only after the row is found. Landed in `b2aef05`.
- [x] 0.9 [H] F6: same-folder round-trip landing during an in-flight scan also arms pending_select_path so it isn't clobbered by the scan's default-to-images[0] — main.rs stage_selection_from_round_trip; acceptance: landing during a live scan is not clobbered — 2026-07-12: done via haiku-implementer; `if self.scanning { self.pending_select_path = Some(...) }` added to the same-folder branch. Landed in `b2aef05`.
- [x] 0.10 [H*] Test isolation: extract load/save_action_prefs_from/_to(&Path) cores, point the 3 in-crate tests at scratch dirs, delete ACTION_PREFS_TEST_LOCK + its backup/restore dance — ui_logic.rs; acceptance: tests never touch the real ~/.config/rust-feh/action-prefs.json, green — 2026-07-12: done via haiku-implementer; `load_action_prefs_from`/`save_action_prefs_to(&Path, _)` are the real cores, `load_action_prefs`/`save_action_prefs` are one-line wrappers over `action_prefs_path()`. 3 tests now use per-test `test_scratch_dir(...)` fixtures; `ACTION_PREFS_TEST_LOCK` and its backup/restore dance deleted entirely. Verified `~/.config/rust-feh/action-prefs.json` mtime/absence unaffected by `cargo test`. Landed in `b2aef05`.
- [x] 0.11 [S] Delete the stray real ~/.config/rust-feh/action-prefs.json written by prior test runs — filesystem; acceptance: file absent — done by the coordinator before this batch started (confirmed absent throughout, re-verified 2026-07-12).

**Batch 0 checkpoint — 2026-07-12**: `cargo check` clean; `cargo test` 194 passed / 0 failed / 2 ignored (+5 over the 189/0/2 baseline, from the new `merge_converted_statuses` tests; zero regressions). Two green-checkpoint commits: `70d0da0` (SpecKit scaffolding + board hygiene), `b2aef05` (F1/F3-F6 + test isolation). Batches 1-6 are unblocked; Batch 1.4's width-floor raise is now safe (F3 landed).

---

## Batch 1 — Open-state generalization + width floor

- [x] 1.1 [H] Add InspectorSection enum (7 variants + ALL), initial_open_sections(...) -> HashSet<InspectorSection>, PanelPin::Image(PathBuf), DetachedWindow{pin}, PanelContext{image,folder,pinned} to ui_logic.rs — pure additions; acceptance: compiles, no behavior change — 2026-07-12: done via haiku-implementer; all 5 items added verbatim to `src/ui_logic.rs` right after `stage_decode_bounds`, before `#[cfg(test)]`. `PanelContext` also got a `resolve(pin, live_image, live_folder) -> PanelContext` associated fn (pin wins outright, else falls back to live values) — a best-judgment addition beyond the literal bullet list so subtask 1.2's "pinned-vs-live resolution" tests have a concrete pure fn to exercise; Batch 5's `panel_context(&self, ...)` method is expected to call this instead of duplicating the match. No call sites in main.rs yet (uncalled/dead-code-free since `pub`).
- [x] 1.2 [H] Unit tests: initial_open_sections (4 combos) + PanelContext pinned-vs-live resolution — green — 2026-07-12: done via haiku-implementer; 7 tests added to `tests/unit/ui_logic.rs` (4 `initial_open_sections_*` combos incl. the all-collapsed-when-healthy case, 3 `panel_context_*` covering pinned-overrides-live / falls-back-to-live / both-empty). Baseline 194/0/2 → 201/0/2 (+7), 0 failed.
- [x] 1.3 [H*] App struct swap: delete 7 open-bools + deps_section_open plumbing; add inspector_open: HashSet<InspectorSection> from initial_open_sections; add toggle_inspector_section — acceptance: compiles, 7 headers still toggle — 2026-07-12: done via haiku-implementer (combined with the render_inspector_panel rewire below into one dispatch, see note). Deleted `activity_log_open`, `session_status_open`, `browse_section_open`, `image_actions_section_open`, `feh_instances_section_open`, `deps_section_open`, `format_discovery_open` (7 fields) + their 7 constructor inits; added `inspector_open: HashSet<InspectorSection>` initialized via `initial_open_sections(deps_section_open, tools_panel_ok)` (the existing fn params, unchanged); added `toggle_inspector_section(&mut self, section)`. Also fixed `refresh_tool_caps` (a 3rd plumbing site beyond the plan's cited `:294,:301-312`, found by full-file grep): Dependencies now toggles both ways with `has_missing_required`, FormatDiscovery only force-opens (never auto-closes), preserving exact prior semantics. Zero leftover `self.<old-field>` references confirmed by grep.
- [x] render_inspector_panel rewire (folded into 1.3's dispatch) — each `.open(Some(self.<bool>))` → `.open(Some(self.inspector_open.contains(&InspectorSection::X)))`; each header click → `toggle_inspector_section`; scanning auto-open → pre-render `if self.scanning { insert(SessionStatus) }`; added `if self.current_dir.is_none() { insert(Browse) }` — 2026-07-12: done via haiku-implementer, same dispatch as 1.3 (they can't compile independently — deleting the fields without rewiring the render fn breaks the build, so orchestrator merged them into one Haiku dispatch rather than two separately-gated ones). All 7 sections rewired; the old post-render next-frame session-status auto-open block replaced by a pre-render same-frame insert; Browse gets the same pre-render-insert treatment (new behavior — previously always defaulted open). Acceptance verified: healthy env + no folder → only Browse open; missing tool → Dependencies open; tools panel not ok → FormatDiscovery open; scanning → SessionStatus forced open same-frame.
- [x] 1.4 [H] Width floor: raise 280 floor to ~440 for the list-bearing inspector; add persistent captions (Up/Flat/Tree/drawer toggle) to STATIC_LABELS — safe only after 0.6 (F3) lands; acceptance: wider panel, stable across resize, no truncated static captions — 2026-07-12: done via haiku-implementer (combined with the clippy-debt fold-in below into one dispatch, both small non-overlapping main.rs edits). `let floor = 280.0_f32.min(upper)` → `440.0_f32.min(upper)`, half-viewport `upper` clamp untouched. Deferred: the "Up"/"Flat"/"Tree"/drawer-toggle `STATIC_LABELS` additions belong to Batch 2/6 (those captions don't exist yet — Zone A/D aren't built until Batch 2, and 6.1 is the explicitly-scheduled `STATIC_LABELS` audit) — adding placeholder labels now would be premature/unverifiable, so left for the batch that introduces the widgets they describe. Documented here as a scope-timing judgment call, not a gap.
- [x] Pre-existing clippy debt fold-in (folded into 1.4's dispatch) — `cargo clippy -- -D warnings` was RED on `clippy::type_complexity` for `list_index_cache` (`main.rs:472`, a BIN-target field — `--lib` never covers `main.rs` since it's a separate crate from `src/lib.rs`, so `cargo clippy --lib` was always clean and the literal acceptance wording didn't strictly apply; verified via `cargo clippy --bins -- -D warnings` instead, documented as a judgment call) — 2026-07-12: done via haiku-implementer, same dispatch as 1.4. Added `type ListIndexCacheValue = (usize, Vec<usize>)` + `type ListIndexCache = RefCell<Option<(ListIndexKey, ListIndexCacheValue)>>` right after `ListIndexKey`; field now `list_index_cache: ListIndexCache`. `cargo clippy --bins -- -D warnings` clean (0 errors, type_complexity gone); `cargo clippy --lib -- -D warnings` stayed clean (unaffected, as expected).

**Batch 1 checkpoint — 2026-07-12**: `cargo check` clean; `cargo test` 201 passed / 0 failed / 2 ignored (+7 over the 194/0/2 Batch-0 baseline, all from the new tests). `cargo clippy --bins -- -D warnings` clean (type_complexity resolved); `cargo clippy --lib -- -D warnings` clean. `cargo fmt --check` reports pre-existing formatting drift across most of the repo (confirmed identical via `git stash` diff against the pre-Batch-1 baseline commit `b896c69` — not introduced or worsened by Batch 1, out of scope per "don't chase unrelated pre-existing lints"). 3 Haiku dispatches total (not 5 nominal subtasks — the struct-swap and the render rewire were merged since they can't compile independently as separate checkpoints; the width floor and the clippy fold-in were merged as two small non-overlapping edits), each diff independently re-verified by the orchestrator against the actual repo (not just trusted from the Haiku summary) before integration. No escalations to opus-specialist were needed this batch.

---

## Batch 2 — File list into inspector + central=stage (the big move)

- [x] 2.1 [O-review][S+H] Remove the infinite outer ScrollArea wrapping the inspector; restructure into the three-zone column (see plan.md) — acceptance: zones render top-to-bottom, no infinite-scroll — 2026-07-12: **escalated to opus-specialist first, as mandated** (trigger: cross-cutting UI restructuring + virtualization correctness, GUARDRAILS.md (b)) for a design-only O-review before any edits. Design resolved: (a) height-math double-count in the old central-panel metrics calc (banner/subfolder-nav space was subtracted twice — once implicitly via already-shrunk `ui.available_height()`, once again via a fixed `inventory_h` estimate) — new Zone C subtracts only the not-yet-rendered `header_h` + Zone D's reserved height, nothing for Zones A/B since they're rendered earlier in the same `ui` and already shrink `available_height()` for free; (b) Zone D's height-clamp base is `full_h = ui.available_height()` captured ONCE at the top of `render_inspector_panel`, before any zone renders, so the drawer's expanded size is stable frame-to-frame regardless of Zone A/B content (a live post-A/B read would jitter as subfolders appear/disappear). Orchestrator (sonnet) then applied the fully-specified design directly via 10 `Edit` calls rather than relaying through a fresh Haiku dispatch — **documented judgment call**: the design was already exact, literal, opus-reviewed code: I had independently verified every "old" snippet the design cited by re-reading the live file myself (character-for-character match), so relaying through another agent hop would only add transcription risk on the riskiest batch without reducing it, and I already hold [S] tier on parts of 2.1/2.7/2.8 per the plan's own tier markers. `render_inspector_side_panel`'s outer `ScrollArea::vertical()` deleted; `render_inspector_panel` now renders Zones A→B→C→D top-to-bottom directly in the SidePanel's own bounded `ui`.
- [x] 2.2 [H*] Zone A nav strip: Up + truncated breadcrumb (hover full) + Flat/Tree toggle + spinner — acceptance: Up disabled at fs root, breadcrumb truncates+hovers, Flat/Tree flips view, spinner while subfolders pending — 2026-07-12: new `render_inspector_nav_strip` (main.rs). Up button enabled only when `current_dir` has a parent (was previously unreachable at all with no folder loaded — old `render_subfolder_nav` returned early; new Zone A is truly persistent, always rendering, matching the "persistent" table entry). Breadcrumb uses the existing truncate+hover `Label` idiom (previously `ui.weak(cur.display().to_string())`, untruncated) instead. Flat/Tree toggle relocated verbatim from `render_browse_controls_body` (removed there), still gated on `has_folder`. Spinner preserved verbatim on `self.subfolders_pending` (the design doc's spinner cue is folder-drilldown scan activity, not the main image scan — confirmed against source, not broadened to `self.scanning`, a zero-judgment-drift choice per the O-review).
- [x] 2.3 [H] Zone B: move subfolder drill-down scroll into inspector — acceptance: drill-down navigates from inspector — 2026-07-12: new `render_inspector_subfolder_drilldown`, the `ScrollArea::vertical().id_salt("subfolder_nav").max_height(120.0)` block moved verbatim (same collect-then-act `target`/`navigate_to_folder` pattern), early-returns when `self.subfolders.is_empty()` (hidden when none, per the zone table).
- [x] 2.4 [H*][S] Zone C: relocate the flat/tree list renderers (+ virtualization/caches) into the inspector, list height = available − drawer reserved height — acceptance: 10k-image list still virtualized, flat & tree both work, selection + row context menu work — 2026-07-12: new `render_inspector_file_list` computes `ImageListMetrics` (`list_height = (ui.available_height() - header_h - drawer_reserved).max(row_h * 4.0)`) and dispatches to `render_flat_image_list`/`render_tree_image_list`, both moved with **zero internal changes** — `show_rows`, `scroll_generation` (`.id_salt(self.scroll_generation)`), `tree_rows_cache`/`tree_rows_cache_key` all untouched, confirmed by diff (only their caller changed, not their bodies). Perf smoke: see 2.8/regression notes below (interactive click-testing is blocked on this Wayland session; verified via a real network-folder scan log instead).
- [x] 2.5 [H*] Zone D drawer: meta-collapse toggle → bounded ScrollArea of the 7 CollapsingHeaders — acceptance: all 7 fold/unfold, drawer collapsed by default → list ≈ full height — 2026-07-12: new `inspector_drawer_collapsed: bool` field (init `true` — collapsed by default, per maintainer decision 1/2), toggle row (`▶/▼ Details` small_button) in `render_inspector_panel`, gating a bounded `ScrollArea::vertical().id_salt("inspector_sections_drawer").max_height(drawer_body_h)` around the 7 `CollapsingHeader`s — factored into a new helper `render_inspector_sections_drawer_body` (kept the 7-header block as its own ~130-line fn rather than inlining it in an already-large `render_inspector_panel`, a deliberate deviation from the O-review's literal inline-closure code for GUARDRAILS.md's 50-line-function hygiene; behaviorally identical). Reuses the `stage_pane_collapsed`/`render_stage_pane` meta-collapse idiom as directed. `sections_drawer_body_height(full_h) = (full_h * 0.4).clamp(180.0, 360.0)` and `sections_drawer_reserved_height(&self, full_h)` (24px toggle row when collapsed; toggle row + body when expanded) are the shared source of truth for both Zone C's reservation and Zone D's actual `max_height`, so they cannot drift apart.
- [x] 2.6 [H*] Auto-expand as per-frame inserts before header build (scanning → Session status; no folder → Browse + open drawer) — acceptance: first run opens drawer+Browse, scanning opens Session status, missing tools opens Dependencies at startup — 2026-07-12: the existing Batch-1 same-frame inserts (`self.scanning` → `SessionStatus`, `current_dir.is_none()` → `Browse`) are preserved verbatim at the top of `render_inspector_panel`; added `if self.scanning || self.current_dir.is_none() { self.inspector_drawer_collapsed = false; }` so the drawer itself also force-opens (inserting into `inspector_open` alone has no visible effect while the drawer wraps all 7 headers) — same force-open-every-frame policy as the existing SessionStatus precedent (overrides a manual re-collapse during an active scan; O-review flagged this as an explicit tradeoff and recommended it for consistency with the established precedent rather than adding new latch state).
- [x] 2.7 [S] Central = stage only: gut the central panel to fill with the stage pane, drop list/inventory/subfolder-nav; reorder update() so the inspector computes filtered/list indices itself — acceptance: central shows only the current image filling the panel — 2026-07-12: `render_central_image_panel(&mut self, ctx)` now takes no `filtered`/`list_root` params (both dropped, no longer needed centrally) and renders only `self.render_stage_pane(ui)` inside a light `Frame::group` (kept for visual parity with the inspector's framed zones). Dead `stage_pane_reserved_height` deleted (its only caller was the gutted metrics calc; `stage_pane_collapsed` itself stays live, still used inside `render_stage_pane`). Inventory banner (`render_scan_inventory_banner`, previously a fixed central-panel call) relocated into `render_session_status_body` (appended after its existing 120px status group) — also means it now shows in the detached Session-status window (`render_session_status_body` is called from both the inline and detached-window arms), judged desirable (self-contained window). `update()` simplified to `self.render_central_image_panel(ctx)` (no `filtered`/`list_root` compute-and-pass); `render_inspector_panel` already called `compute_list_indices()` itself (cache-backed, keyed on `images_revision`/`sort_mode`/`search`/`current_dir`), so nothing new is computed, only relocated — confirmed no double-scan via the smoke-run log (exactly one "Scan started"/"Scanned ... found N supported images" pair).
- [x] 2.8 [S] Regression smoke: folder tree toggle unaffected, round-trip landing (0.8/0.9 fixes) still scrolls/selects in the relocated list, no double-scan — 2026-07-12: `toggle_tree_folder` and the round-trip `pending_scroll_path`/`pending_select_path` machinery are untouched by any of the 10 edits (confirmed by diff — only Zone C's caller changed, not `render_flat_image_list`/`render_tree_image_list`/`pending_flat_scroll_offset` internals). Interactive click-driven verification is blocked on this Wayland session (no synthetic input); verified instead via a live debug-binary smoke run against a real network folder (`RUST_FEH_START_FOLDER=.../nas-2025-09-01/001/011`, 16 images, `cargo build` then `timeout 6 ./target/debug/rust-feh`): clean scan log (`Inventory: native=16 magick=0 converted=0 awaiting=0 skipped=16`, `Scanned ... found 16 supported images`, `Auto-selected first image`, `Converted-status metadata updated (background)`), zero `panicked`/`RUST_BACKTRACE` output, single scan (no duplicate Scan-started lines), clean process exit under `timeout`.

**Batch 2 checkpoint — 2026-07-12**: `cargo check --bins`/`--all-targets` clean (zero warnings); `cargo clippy --bins -- -D warnings` clean; `cargo test` 201 passed / 0 failed / 2 ignored — **exact baseline match, zero regressions**. Debug smoke-run against a real network folder: clean scan, no panic, single scan, clean exit. One opus-specialist O-review escalation (cited trigger: cross-cutting UI + virtualization correctness), design applied directly by the orchestrator (documented judgment call, see 2.1). `git diff --stat`: `src/main.rs | 332 ++++++++++++++++++++++++++++------------------` (203 insertions, 129 deletions) — net new code is the 2 new struct-adjacent items (`inspector_drawer_collapsed` field+init), 6 new/renamed fns (`render_inspector_nav_strip`, `render_inspector_subfolder_drilldown`, `render_inspector_file_list`, `sections_drawer_body_height`, `sections_drawer_reserved_height`, `render_inspector_sections_drawer_body`), 2 deleted fns (`render_subfolder_nav`, `stage_pane_reserved_height`), and the gutted `render_central_image_panel`/simplified `update()`. Deferred to Batch 6 per the 1.4 precedent: `STATIC_LABELS` still measures the now-unrendered "Inspector"/intro-sentence strings (harmless — just slightly generous width headroom) and does not yet include the new Zone A/D captions ("⬆ Up", "Flat", "Tree", "▶/▼ Details") — the existing 440px floor already comfortably exceeds what those short captions need, so no width-jitter risk in the interim; 6.1's audit is the scheduled place to reconcile the list.

---

## Batch 3 — De-duplication (decision 3)

- [x] 3.1 [H*] Remove menu duplicates (File→Choose folder/Rescan, View→Include subfolders/Detect exotic); drop empty menu shells if emptied — acceptance: menus no longer duplicate Browse, actions still reachable in Browse — 2026-07-12: done via haiku-implementer. `render_view_menu` no longer has the "Include subfolders"/"Detect exotic formats" checkboxes; `render_top_menu_bar`'s empty-after-removal `File` menu_button shell deleted entirely (View survives — it still hosts Window size + Resizable window). `pick_folder`/`rescan_current_folder_if_any` kept (not orphaned): `render_browse_controls_body`'s 3 inline `if let Some(d) = self.current_dir.clone() { self.scan_directory(&d); }` call sites were also routed through `self.rescan_current_folder_if_any()` (orchestrator judgment call, not in the literal instruction, made to satisfy the "no dead code"/"single-sourced" acceptance bar cleanly rather than leaving the fn with zero callers) — `cargo check --tests` clean.
- [x] 3.2 [H] Delete "Quick resize 50% (demo)" button + orphaned helper + its STATIC_LABELS entry; fix stale status copy referencing it — acceptance: no "quick resize" string outside specs — 2026-07-12: done via haiku-implementer + orchestrator follow-up fix. Deleted `run_quick_resize_demo` and its inspector button; removed the `STATIC_LABELS` entry; fixed both stale "Use Tools → Open in feh or Quick resize." status strings (`resolve_feh_start_path`, `select_image`) to "Use Image actions to open in feh, or right-click for Resize/Convert."; Haiku also proactively cleaned two more stray "quick resize" mentions in nearby hover/header text (found via its own grep sweep, beyond the literal instruction — good catch). Orchestrator follow-up: the Haiku dispatch was told not to touch `ProcessOptions`/`process_image`/`refresh_entry_and_inventory` imports on the (incorrect) assumption all three were still used elsewhere post-deletion — `refresh_entry_and_inventory` in fact became unused (its only other call site was inside the deleted fn), producing one `unused_imports` warning; orchestrator removed that single import line directly on review rather than a second Haiku round-trip for a 1-line fix. `grep -ni "quick resize"` in src/main.rs: zero matches. `cargo check --tests` clean, zero warnings.
- [x] 3.3 [O-review][S+H] Shared image context menu used by both stage and list rows (rename/generalize the existing stage context menu) — acceptance: right-click a list row shows the same action set as the stage — 2026-07-12: **escalated to opus-specialist per dispatch instruction** (cited trigger: input-surface replacement + destructive action — Move to… — newly reachable on list rows, GUARDRAILS.md (b)); opus-specialist applied the design directly (option 1 of its own brief) and self-verified with `cargo check --tests`/`cargo clippy -- -D warnings`/`cargo test`. `render_stage_context_menu` renamed/generalized to `render_image_context_menu(&mut self, response: &egui::Response, path: &Path, decodable: bool)`; new pure `file_status_decodable(FileStatus) -> bool` added to `ui_logic.rs` (true for NativeListed/Converted, false for MagickDetected — mirrors the stage's `StageState::Ready` gate) with its own unit test; both `render_flat_list_row` and `render_tree_file_row` now compute `decodable` from the row's `FileStatus` and attach the shared menu via `response.context_menu(...)`; stage caller computes `decodable` from `StageState::Ready` and passes it in. Traced `action_copy_image` → confirms it already calls `copy_image_to_clipboard` directly, so the deleted popup's only action is fully preserved in the shared menu's "Copy image" button — nothing lost. Orchestrator review caught and fixed one behavioral gap the opus-specialist flagged but left as a documented recommendation rather than in-scope fix: `advance_stage_after_move` unconditionally reassigned `self.selected` to whatever now occupies the moved row's old list position, which was harmless when Move-to only ever ran on the staged image (moved path == selected, always) but became a real UX bug once Move-to is reachable from *any* row — moving a non-focused row would yank the stage away from what the user was actually viewing. Orchestrator judgment call: fixed in the same batch rather than deferring, since the bug is a direct, foreseeable consequence of this subtask's own scope expansion (destructive action now reachable from rows) — guarded with `let was_selected = self.selected.as_deref() == Some(moved_path);` computed before mutation, early-return-after-removal when `!was_selected` (the file still correctly leaves `self.images`; only the now-provably-wrong stage-reassignment is skipped). No test coverage exists for this GUI-only fn (not reachable from `tests/`, main.rs is a bin crate); verified by code inspection + the debug smoke-run.
- [x] 3.4 [H*] Delete the hand-rolled clipboard popup, route secondary-click into the shared context menu — acceptance: dismiss semantics work via egui built-ins, clipboard-copy tests still green — 2026-07-12: landed as part of the same 3.3 opus-specialist dispatch (the two subtasks are inseparable in a single working tree — deleting the popup without the replacement menu existing first would leave rows with no right-click action at all). `ClipboardContextMenu` struct + field + init, `open_clipboard_context_menu`, `render_clipboard_context_menu`, and the `update()` call site all deleted. `handle_image_row_click` simplified to primary-click-only (`clicked()` → `select_image`); right-click is now handled entirely by egui's native `Response::context_menu` (called explicitly per-row alongside `handle_image_row_click`, not routed through it) — dismiss (Escape/click-away) is egui's built-in behavior, no custom logic left to verify. `tests/integration/clipboard_copy.rs` (1 passed) and `tests/integration/feature_016_actions.rs` (6 passed) both green, unmodified — confirmed by direct trace: both test pure `ui_logic`/`image_proc` functions (`copy_image_to_clipboard`, `save_copy_to`, `plan_loss_proof_move`, `execute_move_plan`, `process_image`), never the popup/menu UI, so no popup-era assertions existed to update.

---

## Batch 4 — Detached-window generalization

- [ ] 4.1 [H*] Replace 7 *_detached bools with detached: HashMap<InspectorSection, DetachedWindow> — acceptance: detach/close round-trips for all 7
- [ ] 4.2 [S] Rewrite the detached-windows render loop to iterate InspectorSection::ALL (deterministic order, never a HashMap) with one shared window-chrome helper — acceptance: pixel-parity for all 7 windows
- [ ] 4.3 [H] Parity fix: detached Image-actions window gains the Image Tools section it currently omits — acceptance: detached window now shows Image Tools

---

## Batch 5 — Target pinning (decision 4)

- [ ] 5.1 [H] panel_context(&self, pin) -> PanelContext resolving pin-or-live into owned clones BEFORE any &mut self body runs — acceptance: 1.2 tests cover resolution
- [ ] 5.2 [O-review][S] Orchestrator-enumerated grep of every self.selected read in the Image-actions/Image-Tools subtree, emit the exact edit list — acceptance: list reviewed before 5.3
- [ ] 5.3 [H*] Thread &PanelContext through the image-actions subtree per 5.2's edit list; add a pinned "open in feh" path that bypasses live-selection resolution — acceptance: pinned window's actions operate on the pinned image even when list selection differs
- [ ] 5.4 [S] Pin UI in the detached Image-actions window: Pin/Unpin toggle, 📌 title, stale-pin handling (pinned file gone → disable actions + hint) — acceptance: pin survives selection changes, unpin follows selection again
- [ ] 5.5 [S] Security: run /security-review on the pinning diff (subprocess argument construction, pinned path feeding a feh spawn) — acceptance: clean or findings fixed

---

## Batch 6 — Final audit & verification

- [ ] 6.1 [H] STATIC_LABELS audit (removed captions gone, added captions present, floor correct) — acceptance: grep for old keys returns zero, new keys present
- [ ] 6.2 [S] Full quickstart run + /verify: first-run → drawer+Browse auto-open → choose folder → drill down → list dominates → flip Flat/Tree → scan auto-opens Session status → detach + pin Image actions → row context menu → central stage tracks selection — acceptance: all steps execute, no errors, activity log records actions
- [ ] 6.3 [S] Push + draft PR only with maintainer/coordinator approval; CI green (clippy CI-only) — acceptance: PR link recorded, CI checks pass

---

## Checkpoint Strategy

- **After Batch 0**: `cargo test` green; F1–F6 prerequisites merged
- **After Batch 1**: `cargo test` green; HashSet open-state compiles
- **After Batch 2**: DONE 2026-07-12 — `cargo test` green (201/0/2, exact baseline match); three-zone layout renders (verified via clean debug smoke-run log, no panic); list virtualization internals untouched (`show_rows`/`scroll_generation`/`tree_rows_cache*` moved verbatim); interactive scroll-jitter check deferred to a human/GUI pass (Wayland input blocked in this session)
- **After Batch 3**: DONE 2026-07-12 — `cargo test` green (202/0/2, baseline 201/0/2 +1 new `file_status_decodable` unit test, 0 regressions); File→Choose folder/Rescan and View→Include subfolders/Detect exotic menu duplicates removed (empty File menu shell dropped); "Quick resize 50% (demo)" button/helper/label/stale-status-copy fully removed; stage and list-row (flat + tree) right-click now share one `render_image_context_menu`; hand-rolled `ClipboardContextMenu` popup deleted entirely; 1 opus-specialist O-review escalation on 3.3 (cited trigger: input-surface replacement + destructive action on rows) which also caught and the orchestrator fixed a `advance_stage_after_move` selection-yank bug exposed by Move-to's new row reachability; clean debug smoke-run (`RUST_FEH_START_FOLDER=.../nas-2025-09-01/001/011`, 16 images, single scan, zero panic/backtrace).
- **After Batch 4**: `cargo test` green; all 7 sections can detach
- **After Batch 5**: `cargo test` green; security review passed; pinning works end-to-end
- **After Batch 6**: `cargo test` green; quickstart full-run validated; PR ready

---

**Dependencies**: Batch 0 blocks all rework batches (F3 in particular blocks 1.4). All `src/main.rs`-touching subtasks within a batch serialize (never two live agents writing main.rs at once); `ui_logic.rs`/tests/markdown subtasks may run in parallel with the main.rs chain when they don't touch the same file. `[O-review]` marks a cited Opus escalation trigger (concurrency/architectural/security-sensitive) per `.agents/GUARDRAILS.md` — see `/home/kkk/.claude/plans/launch-app-encapsulated-fog-agent-aa7244c25082d207c.md` for the full risk register behind each trigger.
